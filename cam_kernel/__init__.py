"""Top-level marker for the cam_kernel package.

Note: The actual Python package lives in ``cam_kernel/cam_kernel/`` (the
inner directory).  The outer ``cam_kernel/`` directory is also used for
project metadata (``pyproject.toml``, ``web_ui.bat``, ``jobs/``,
``native/``, ``tests/``).  This file makes the outer directory a
proper Python package so the inner package is reachable as
``cam_kernel.cam_kernel`` without namespace package complications.
"""
